# Google Form Track Decision Note — Sugamya Sahayak

**Status:** Decision note only. No track has been selected or submitted anywhere. This is **Pending CEO Decision #2**, already recorded in `RULES.md` §1a — this note does not resolve it, and does not guess an answer.

## Why this isn't guessed
The Google Form requires selecting exactly one of **AI Automation / AI Agent / Product Build** (per `docs/SUBMISSION.md`). This project's architecture spans two of the source document's track categories: the Telegram+n8n path fits AI Agent/Automation, while the required public website fits Product Build. `RULES.md` §1a states the "one person, one project, one track" rule is mandatory, and explicitly logs this as a CEO decision, not one this session may make unilaterally.

## The three options, as already recorded in `RULES.md` §1a
- **(a)** Declare **AI Agent** or **AI Automation** as the single track; treat the website as a non-scored bonus/demo convenience, de-emphasized in the submission narrative.
- **(b)** Declare **Product Build** as the single track; treat the Telegram/n8n path as a supporting implementation detail of that product, not a separately-scored track element.
- **(c)** Present a hybrid, pick one track for the form field while still building/describing both channels, and disclose the choice honestly in Q3 of the submission form.

## This session's observation (not a recommendation to override the CEO)
Given that the Telegram channel is the **only** live-verified, fully working channel (per `docs/SUBMISSION.md`/`docs/submission_evidence.md`), and the web UI is explicitly offline reference content only, option **(a)** would most closely match what has actually been proven working — but this is an observation for the CEO's consideration, not a decision made here.

## What happens next
The CEO selects (a), (b), or (c) (or another approach). Once selected, this session (or the CEO) updates `RULES.md` §1a's "Pending CEO Decision #2" to record the choice and timestamp, and `docs/SUBMISSION.md`'s track field is finalized accordingly. No Google Form field has been filled in or submitted.
